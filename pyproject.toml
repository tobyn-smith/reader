[project]
name = "vault"
version = "0.2.0"
description = "syllabus intake and reading pipeline for coursework"
requires-python = ">=3.11"
dependencies = [
    "pymupdf>=1.24",
    "pdfplumber>=0.11",
    "psycopg[binary]>=3.1",
    "python-dotenv>=1.0",
    "wordninja>=2.0",
    "requests>=2.31",
    "rich>=13.7",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0"]

[project.scripts]
vault = "vault.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
vault = ["llm/prompts/*.md"]

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["ignore::DeprecationWarning"]
markers = [
    "db: needs a live postgres (skipped unless DATABASE_URL is set)",
    "network: hits an external api (skipped unless VAULT_ALLOW_NETWORK=1)",
]
