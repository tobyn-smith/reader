.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.env
.env.local

# on-disk caches and scratch output
.cache/
out/
*.parse.json

# next.js
web/node_modules/
web/.next/
web/out/
web/.vercel

# built sites are outputs, never source. the private one holds full text.
_site_public/
_site_private/
data/private*
app/vendor/
_site/
build/
dist/
corpus*.json
scrub.txt

# local tooling. the launch config says how to serve the app and is worth
# keeping; everything else in here is scratch, including whole throwaway
# checkouts of the repo, which must never be committed back into it.
.claude/*
!.claude/launch.json
